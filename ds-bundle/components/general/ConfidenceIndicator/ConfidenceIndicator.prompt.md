ConfidenceIndicator from @smartervote/design-system. Use via `window.SmarterVoteDS.ConfidenceIndicator` (bundle loaded from the root `_ds_bundle.js`).

Glowing-dot confidence pill (high/medium/low/unknown) used throughout
candidate issue stances and data-quality indicators.

## Props

```ts
interface ConfidenceIndicatorProps {
  confidence: "high" | "medium" | "low" | "unknown";
}
```
