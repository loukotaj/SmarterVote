# Pre-commit hooks for SmarterVote
# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: check-toml

  - repo: https://github.com/psf/black
    rev: 23.12.1
    hooks:
      - id: black
        language_version: python3
        files: ^(pipeline|services|scripts|tests)/.*\.py$

  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        args: ["--profile", "black"]
        files: ^(pipeline|services|scripts|tests)/.*\.py$

  # Note: flake8 and mypy are run in CI but not as pre-commit hooks
  # to avoid blocking commits on legacy code issues
  # Run manually with: pre-commit run flake8 --all-files
  # - repo: https://github.com/pycqa/flake8
  #   rev: 7.0.0
  #   hooks:
  #     - id: flake8
  #       args: ["--max-line-length=127", "--extend-ignore=E203,W503"]
  #       files: ^(pipeline|services|scripts|tests)/.*\.py$

  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.8.0
  #   hooks:
  #     - id: mypy
  #       files: ^(pipeline|services|scripts)/.*\.py$
  #       additional_dependencies: [types-requests, types-PyYAML]
  #       args: ["--ignore-missing-imports"]
