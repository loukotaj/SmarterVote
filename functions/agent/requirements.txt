# Cloud Functions entry point
functions-framework==3.8.1

# HTTP client
httpx>=0.28.1,<1.0.0

# Google Cloud SDKs
google-cloud-firestore==2.16.0
google-cloud-storage==2.19.0
google-auth>=2.48.1

# pipeline_client / shared package dependencies
# (those packages are bundled in the source zip, but their pip deps must
#  be listed here so the Cloud Functions build step installs them)
pydantic>=2.9.0
pydantic-settings==2.4.0
openai>=1.84.0
requests>=2.28.1
tenacity>=8.2.3
