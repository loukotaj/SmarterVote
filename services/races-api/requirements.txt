# Core API framework
fastapi==0.112.0
starlette>=0.37.2,<0.38.0
uvicorn[standard]==0.30.1
pydantic==2.9.0

# Rate limiting
slowapi==0.1.9

# HTTP client (analytics proxy + Auth0 JWKS)
httpx>=0.28.1,<1.0.0

# JWT verification for Auth0
python-jose[cryptography]==3.3.0

# Google Cloud Storage (for reading race data in production)
google-cloud-storage==2.19.0

# Google Cloud Firestore (for persistent analytics in production)
google-cloud-firestore==2.16.0

# Google Cloud BigQuery (for querying the Cloud Billing export — GCP cost dashboard)
google-cloud-bigquery==3.25.0

# Local shared schema package
# Install with: pip install -e ../../shared
