# SmarterVote - All Python Dependencies
# Install with: pip install -r requirements.txt

# Pipeline Client (FastAPI backend)
fastapi==0.112.0
uvicorn[standard]==0.30.1
pydantic==2.8.2
pydantic-settings==2.4.0
python-multipart==0.0.9
websockets==12.0
httpx==0.27.0
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1

# Races API
# (uses fastapi/uvicorn/pydantic already above)

# Pipeline V2 Agent (uses httpx for all API calls)
# httpx already above

# Shared models
# pydantic already above

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
