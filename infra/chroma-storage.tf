# ChromaDB storage removed — V2 pipeline does not use a vector database.
# This file is intentionally empty. It remains to avoid Terraform state issues
# if the chroma_storage bucket was previously created. Run `terraform state rm`
# for the old resources after migrating.
