# Python dependencies for the SmarterVote pipeline
# Only the search cache utility remains from the V1 pipeline.
# The V2 agent lives in pipeline_v2/ and uses pipeline_client/backend/requirements.txt.

# Core
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
